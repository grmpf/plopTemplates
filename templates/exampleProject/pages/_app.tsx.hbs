import '../src/styles/global.scss'
import '../src/styles/typography.scss'

const App = ({ Component, pageProps }) => {
  return (
      <>
        <Component {...pageProps} />
      </>
  )
}

export default App
