import {Suspense, useEffect, useLayoutEffect, useMemo, useRef, useState} from "react";

const HomePage = ({post, redirect, preview}) => {
	return (
		<>
			{{name}}
			<hr />
			{{desc}}
		</>
	)
}

export default HomePage
